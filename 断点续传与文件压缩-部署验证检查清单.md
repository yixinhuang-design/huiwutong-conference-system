# 断点续传与文件压缩 - 部署验证检查清单

> ✅ 此清单用于验证断点续传和文件压缩功能的完整部署和测试
> 执行日期: __________ | 执行人: __________ | 项目: 智能排座系统

---

## 📋 Part 1: 后端文件部署检查

### 1.1 配置类部署
- [ ] 文件位置: `conference-meeting/src/main/java/com/conference/meeting/config/FileUploadProperties.java`
- [ ] 文件存在: ✓
- [ ] 注解检查: 
  - [ ] @Configuration
  - [ ] @ConfigurationProperties(prefix = "file.upload")
  - [ ] 所有属性有@Value或@ConfigurationProperties注入
- [ ] 编译无错误: ✓
- [ ] IDE无红波浪线: ✓

### 1.2 DTO类部署
- [ ] ChunkUploadRequest.java
  - [ ] 位置: `dto/ChunkUploadRequest.java`
  - [ ] 包含所有字段: uploadId, chunkIndex, totalChunks, chunkSize, totalSize, fileHash, chunkHash, fileName, scheduleId, description
  - [ ] Lombok注解: @Data @Builder @NoArgsConstructor @AllArgsConstructor
  - [ ] 编译无错: ✓

- [ ] ChunkUploadResponse.java
  - [ ] 位置: `dto/ChunkUploadResponse.java`
  - [ ] 包含字段: uploadId, chunkIndex, success, message, tempPath, uploadedChunks, totalChunks, progress, attachment
  - [ ] Lombok注解完整: ✓

- [ ] UploadProgressResponse.java
  - [ ] 位置: `dto/UploadProgressResponse.java`
  - [ ] 包含字段: uploadId, fileName, totalSize, totalChunks, uploadedChunks(List<Boolean>), uploadedCount, progress, status, uploadedSize, estimatedTime
  - [ ] 编译无错: ✓

- [ ] UploadStatsResponse.java
  - [ ] 位置: `dto/UploadStatsResponse.java`
  - [ ] 包含字段: uploadId, fileName, totalSize, uploadedSize, totalChunks, uploadedChunks, progress, status, estimatedTime, uploadSpeed, elapsedTime
  - [ ] 编译无错: ✓

### 1.3 工具类部署
- [ ] FileCompressionUtil.java
  - [ ] 位置: `conference-meeting/src/main/java/com/conference/meeting/util/FileCompressionUtil.java`
  - [ ] 包含方法:
    - [ ] shouldCompress(String fileType)
    - [ ] compressImage(Path input, Path output, int quality)
    - [ ] compressPdf(Path input, Path output)
    - [ ] compressFile(Path input, Path output, String fileType)
    - [ ] estimateCompressedSize(long originalSize, String fileType)
  - [ ] 导入依赖:
    - [ ] java.awt.image.BufferedImage
    - [ ] javax.imageio.ImageIO
    - [ ] java.nio.file.*
  - [ ] 编译无错: ✓

### 1.4 服务接口部署
- [ ] IChunkUploadService.java
  - [ ] 位置: `service/IChunkUploadService.java`
  - [ ] 方法签名:
    - [ ] initializeUpload(String, String, Long, String, Long): UploadProgressResponse
    - [ ] uploadChunk(ChunkUploadRequest, MultipartFile): ChunkUploadResponse
    - [ ] queryProgress(String): UploadProgressResponse
    - [ ] mergeChunks(String, Long, String): Object
    - [ ] cancelUpload(String): void
    - [ ] cleanExpiredTempFiles(): void
  - [ ] 编译无错: ✓

### 1.5 服务实现部署
- [ ] ChunkUploadServiceImpl.java
  - [ ] 位置: `service/impl/ChunkUploadServiceImpl.java`
  - [ ] 注解: @Service @Transactional
  - [ ] 依赖注入:
    - [ ] @Autowired FileUploadProperties uploadProperties
    - [ ] @Autowired ScheduleAttachmentMapper scheduleAttachmentMapper
    - [ ] @Autowired ScheduleMapper scheduleMapper
    - [ ] @Autowired IScheduleAttachmentService scheduleAttachmentService
    - [ ] @Autowired FileCompressionUtil fileCompressionUtil
  - [ ] 实现所有IChunkUploadService方法: ✓
  - [ ] 包含辅助方法:
    - [ ] createTempUploadDir(String)
    - [ ] getTempUploadDir(String)
    - [ ] deleteTempUploadDir(String)
    - [ ] calculateHash(MultipartFile)
    - [ ] getFileExtension(String)
    - [ ] calculateProgress(UploadTaskMetadata)
    - [ ] buildProgressResponse(UploadTaskMetadata)
  - [ ] 日志记录完整: log.info(), log.error(), log.debug()
  - [ ] 编译无错: ✓

### 1.6 控制器部署
- [ ] ChunkUploadController.java
  - [ ] 位置: `controller/ChunkUploadController.java`
  - [ ] 注解: @RestController @RequestMapping("/api/schedule/chunk")
  - [ ] API端点:
    - [ ] POST /api/schedule/chunk/init
    - [ ] POST /api/schedule/chunk/upload
    - [ ] GET /api/schedule/chunk/progress/{uploadId}
    - [ ] POST /api/schedule/chunk/finish
    - [ ] DELETE /api/schedule/chunk/{uploadId}
    - [ ] GET /api/schedule/chunk/stats/{uploadId}
    - [ ] GET /api/schedule/chunk/health
  - [ ] 所有方法返回 Result<T> 类型: ✓
  - [ ] 错误处理: try-catch with log.error(): ✓
  - [ ] 编译无错: ✓

---

## 🔨 Part 2: 编译和打包检查

### 2.1 编译检查
```bash
# 在项目根目录执行
mvn clean compile -DskipTests
```

- [ ] 编译成功，无ERROR
- [ ] 警告数合理（可接受）
- [ ] 生成的.class文件:
  - [ ] FileUploadProperties.class ✓
  - [ ] ChunkUploadRequest.class ✓
  - [ ] ChunkUploadResponse.class ✓
  - [ ] UploadProgressResponse.class ✓
  - [ ] UploadStatsResponse.class ✓
  - [ ] FileCompressionUtil.class ✓
  - [ ] IChunkUploadService.class ✓
  - [ ] ChunkUploadServiceImpl.class ✓
  - [ ] ChunkUploadController.class ✓

### 2.2 打包检查
```bash
mvn clean package -DskipTests
```

- [ ] 打包成功
- [ ] 生成JAR文件: target/conference-meeting-*.jar ✓
- [ ] JAR大小合理（无异常增长）

---

## ⚙️ Part 3: 配置文件检查

### 3.1 application.properties 检查
```properties
# 位置: src/main/resources/application.properties
```

需要添加或验证以下配置:

```properties
# 文件上传配置
file.upload.max-file-size=104857600
file.upload.chunk-size=5242880
file.upload.temp-dir=uploads/.temp
file.upload.upload-dir=uploads
file.upload.allowed-extensions=ppt,pptx,pdf,doc,docx,xls,xlsx,txt,jpg,jpeg,png,gif
file.upload.enable-image-compress=true
file.upload.image-quality=85
file.upload.enable-pdf-compress=false
file.upload.temp-file-expire-time=86400000
file.upload.enable-resume-upload=true
file.upload.max-retry-count=3
file.upload.retry-delay=1000
```

验证:
- [ ] 所有配置项存在
- [ ] 数值合理（特别是max-file-size和chunk-size）
- [ ] 格式正确（无空格、无中文注释）
- [ ] 可选项都有默认值

### 3.2 目录创建检查
```bash
# 项目根目录下创建必要目录
mkdir -p uploads/.temp
chmod -R 755 uploads/
```

- [ ] uploads目录存在: ✓
- [ ] uploads/.temp目录存在: ✓
- [ ] 目录权限正确（可读写）: ✓

---

## 🚀 Part 4: 运行环境检查

### 4.1 启动应用
```bash
java -jar target/conference-meeting-*.jar
```

或在IDE中Run Application

- [ ] 应用启动成功
- [ ] 启动日志:
  - [ ] 看到"Tomcat started on port 8084"
  - [ ] 看到"Spring Application started successfully"
  - [ ] 看到FileUploadProperties被加载（可在日志中搜索）

### 4.2 依赖检查
```bash
# 查看Spring是否识别了ChunkUploadController
curl http://localhost:8084/api/schedule/chunk/health
```

- [ ] 返回成功响应（200 OK）
- [ ] 响应体: `{"code":200, "message":"success", "data":"分块上传服务正常运行"}`

---

## 📱 Part 5: 前端文件部署检查

### 5.1 API文件部署
- [ ] 文件位置: `admin-pc/public/js/api/schedule-chunk-upload-api.js`
- [ ] 文件大小: ~600 lines
- [ ] 包含模块:
  - [ ] ChunkUploadAPI 对象
    - [ ] initializeUpload()
    - [ ] uploadChunk()
    - [ ] queryProgress()
    - [ ] finishUpload()
    - [ ] cancelUpload()
    - [ ] getUploadStats()
    - [ ] checkHealth()
  - [ ] FileUploadUtils 工具类
    - [ ] calculateFileHash()
    - [ ] calculateChunkHash()
    - [ ] splitFileToChunks()
    - [ ] formatFileSize()
    - [ ] formatTime()
    - [ ] getFileExtension()
    - [ ] validateFile()
  - [ ] ChunkUploadManager 类
    - [ ] start()
    - [ ] pause()
    - [ ] resume()
    - [ ] cancel()
    - [ ] getProgress()

### 5.2 HTML集成检查
- [ ] 在schedule-mgr.html中添加脚本引入:
  ```html
  <script src="/js/api/schedule-chunk-upload-api.js"></script>
  ```
- [ ] 位置: 在closing </body> 标签之前
- [ ] 编码: UTF-8 (确保中文注释正确显示)

---

## 🧪 Part 6: 功能测试

### 6.1 初始化上传测试
```bash
# 测试初始化端点
curl -X POST 'http://localhost:8084/api/schedule/chunk/init?fileName=test.pptx&totalSize=104857600&fileHash=abc123&scheduleId=1001' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

- [ ] 返回200 OK
- [ ] 响应包含uploadId
- [ ] 响应包含totalChunks: 20 (100MB ÷ 5MB)
- [ ] 响应包含progress: 0

### 6.2 上传分块测试
```bash
# 创建测试分块文件
dd if=/dev/zero of=chunk.dat bs=1M count=5

# 上传分块
curl -X POST 'http://localhost:8084/api/schedule/chunk/upload' \
  -F 'uploadId=YOUR_UPLOAD_ID' \
  -F 'chunkIndex=0' \
  -F 'totalChunks=20' \
  -F 'totalSize=104857600' \
  -F 'fileHash=abc123' \
  -F 'chunkHash=def456' \
  -F 'fileName=test.pptx' \
  -F 'scheduleId=1001' \
  -F 'chunk=@chunk.dat' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

- [ ] 返回200 OK
- [ ] 响应success: true
- [ ] 响应uploadedChunks: 1
- [ ] 响应progress: 5

### 6.3 进度查询测试
```bash
curl 'http://localhost:8084/api/schedule/chunk/progress/YOUR_UPLOAD_ID' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

- [ ] 返回200 OK
- [ ] 返回当前上传进度
- [ ] 返回已上传分块列表

### 6.4 取消上传测试
```bash
curl -X DELETE 'http://localhost:8084/api/schedule/chunk/YOUR_UPLOAD_ID' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

- [ ] 返回200 OK
- [ ] 临时文件被清理: `uploads/.temp/YOUR_UPLOAD_ID` 不存在

### 6.5 完整上传流程测试（浏览器）

1. 打开schedule-mgr.html页面
2. 导航到课件上传功能
3. 选择测试文件（5-50MB）
4. 监控:
   - [ ] 文件验证成功
   - [ ] 进度条显示 0% → 100%
   - [ ] 上传速度实时显示（如 "25.50 MB/s"）
   - [ ] 剩余时间更新准确
   - [ ] 上传完成后文件出现在列表中
   - [ ] 不刷新页面的情况下进度继续

5. 测试暂停/继续:
   - [ ] 点击暂停按钮，进度条停止
   - [ ] 点击继续按钮，进度条继续
   - [ ] 进度和速度恢复

6. 测试取消:
   - [ ] 点击取消按钮
   - [ ] 进度条清零
   - [ ] 检查 uploads/.temp/{uploadId} 目录不存在

---

## 📊 Part 7: 文件系统验证

### 7.1 上传目录结构检查
```bash
# 上传成功后，验证文件结构
find uploads -type f -name "*.pptx" | head -10
```

应该看到类似结构:
```
uploads/
├── 2030309010523144194/              # tenantId
│   └── schedule/
│       └── 1001/                     # scheduleId
│           └── 20240101/
│               └── 1704067200000_课件.pptx
└── .temp/
    └── (clean if upload completed)
```

- [ ] 目录层级正确
- [ ] 文件名格式正确（时间戳_原始名称）
- [ ] 文件可以下载: `uploads/tenantId/schedule/scheduleId/date/file` ✓

### 7.2 临时文件清理检查
```bash
# 24小时后（或手动调用清理接口），检查临时文件是否被清理
ls -la uploads/.temp/
```

- [ ] 超过24小时的上传任务目录已删除
- [ ] 最近的上传任务目录仍然存在（如果未清理）
- [ ] 无孤立的分块文件

### 7.3 文件大小统计
```bash
# 检查压缩效果
du -sh uploads/
ls -lh uploads/2030309010523144194/schedule/*/20*/ | awk '{print $5, $9}'
```

- [ ] 计算压缩率（原始大小 vs 存储大小）
- [ ] 图片文件通常压缩到30-40%
- [ ] PDF通常压缩到50-70%

---

## 🔍 Part 8: 日志检查

### 8.1 启动日志验证
```bash
# 查看启动日志
tail -f logs/application.log | grep -i "upload\|chunk\|compress"
```

应该看到:
- [ ] "初始化上传任务" 日志出现
- [ ] "上传分块" 日志出现
- [ ] "分块上传成功" 日志出现
- [ ] "分块合并完成" 日志出现
- [ ] "文件压缩成功" 日志出现
- [ ] "附件保存成功" 日志出现

### 8.2 错误日志检查
```bash
# 检查是否有ERROR或异常
grep ERROR logs/application.log | tail -20
```

- [ ] 没有与分块上传相关的ERROR
- [ ] 没有FileUploadProperties加载失败的ERROR
- [ ] 没有权限拒绝的ERROR

### 8.3 DEBUG日志（可选）
```bash
# 启用DEBUG日志，查看详细信息
# 修改application.properties:
# logging.level.com.conference.meeting=DEBUG

grep -i "debug" logs/application.log | grep chunk | head -10
```

- [ ] 看到分块索引信息
- [ ] 看到文件哈希计算日志
- [ ] 看到临时文件路径日志

---

## 🔐 Part 9: 安全和权限检查

### 9.1 租户隔离验证
```bash
# 使用不同的Authorization token，确保无法访问其他租户的文件
curl 'http://localhost:8084/api/schedule/attachment/1?tenantId=9999' \
  -H 'Authorization: Bearer OTHER_TENANT_TOKEN'
```

- [ ] 返回403 Forbidden 或 401 Unauthorized
- [ ] 无法看到其他租户的课件

### 9.2 文件类型白名单验证
```bash
# 尝试上传不允许的文件类型（如.exe）
```

- [ ] 上传被拒绝
- [ ] 返回错误信息: "不支持的文件类型"

### 9.3 文件大小限制验证
```bash
# 尝试上传>100MB的文件
```

- [ ] 上传被拒绝
- [ ] 返回错误信息: "文件大小超过限制"

### 9.4 哈希验证
```bash
# 模拟分块传输出错，更改分块哈希
```

- [ ] 合并时检测到哈希不匹配
- [ ] 拒绝合并，返回错误

---

## 📈 Part 10: 性能测试

### 10.1 单个小文件上传（5MB）
- [ ] 开始时间: __________
- [ ] 结束时间: __________
- [ ] 耗时: __________秒
- [ ] 预期: < 10秒
- [ ] ✓ 通过 / ✗ 失败

### 10.2 中等文件上传（50MB）
- [ ] 开始时间: __________
- [ ] 结束时间: __________
- [ ] 耗时: __________秒
- [ ] 预期: 40-80秒
- [ ] ✓ 通过 / ✗ 失败

### 10.3 大文件上传（100MB）
- [ ] 开始时间: __________
- [ ] 结束时间: __________
- [ ] 耗时: __________秒
- [ ] 预期: 80-160秒
- [ ] ✓ 通过 / ✗ 失败
- [ ] 内存占用: __________MB (预期 < 500MB)
- [ ] CPU占用: _________% (预期 < 80%)

### 10.4 并发上传测试
```bash
# 同时上传3个文件
for i in {1..3}; do
  curl -X POST ... &
done
wait
```

- [ ] 3个文件并发上传成功
- [ ] 上传速度未显著下降
- [ ] 没有内存溢出或超时

### 10.5 文件压缩效果测试
- [ ] 上传一张4000×3000的JPG (原始: 8MB)
  - [ ] 压缩后大小: __________MB
  - [ ] 压缩率: __________%
  - [ ] 图片质量: 可接受 ✓ / 失真 ✗

---

## ✨ Part 11: 用户体验测试

### 11.1 UI反馈
- [ ] 上传时显示进度条
- [ ] 进度条平滑更新（不卡顿）
- [ ] 显示上传速度
- [ ] 显示预计完成时间
- [ ] 显示已上传/总文件大小

### 11.2 交互功能
- [ ] 可以暂停上传
- [ ] 暂停后可以继续
- [ ] 可以取消上传
- [ ] 取消后可以重新上传
- [ ] 右上角显示上传状态提示

### 11.3 网络中断恢复
- [ ] 上传中断网络（模拟）
- [ ] 应用自动重试
- [ ] 3次重试后仍失败，显示失败提示
- [ ] 用户可以点击"重试"继续
- [ ] 不需要重新选择文件

### 11.4 完成提示
- [ ] 上传100%后显示成功提示
- [ ] 文件立即显示在课件列表中
- [ ] 可以立即下载已上传的文件
- [ ] 显示上传时间和文件大小

---

## 🎯 Part 12: 最终验收

### 12.1 功能完整性
- [ ] 初始化上传功能 ✓
- [ ] 分块上传功能 ✓
- [ ] 进度查询功能 ✓
- [ ] 完成合并功能 ✓
- [ ] 上传取消功能 ✓
- [ ] 图片压缩功能 ✓
- [ ] 临时文件清理功能 ✓
- [ ] 多文件并发上传 ✓

### 12.2 质量指标
- [ ] 编译无ERROR
- [ ] 单元测试通过率: __________%
- [ ] 集成测试通过率: __________%
- [ ] 没有高危漏洞（安全扫描）
- [ ] 代码规范检查通过

### 12.3 文档完整性
- [ ] 部署指南文档: ✓
- [ ] API文档: ✓
- [ ] 使用示例: ✓
- [ ] 故障排查指南: ✓
- [ ] 配置说明: ✓

### 12.4 验收签字

| 角色 | 姓名 | 日期 | 签字 |
|------|------|------|------|
| 开发人员 | __________ | __________ | __________ |
| 测试人员 | __________ | __________ | __________ |
| 项目经理 | __________ | __________ | __________ |
| 客户代表 | __________ | __________ | __________ |

---

## 📌 已知问题和限制

| 问题 | 严重程度 | 状态 | 备注 |
|------|--------|------|------|
| PDF压缩未集成 | 低 | 计划中 | 需要集成PDFBox库 |
| 服务重启后上传会丢失 | 中 | 计划中 | 考虑使用Redis/数据库存储 |
| 不支持秒传 | 低 | 计划中 | 可基于文件哈希实现 |
| 上传带宽不可限制 | 低 | 计划中 | 可添加限流功能 |

---

## 📚 附录：快速命令参考

### 启动应用
```bash
cd backend/conference-backend/conference-meeting
mvn clean package -DskipTests
java -jar target/conference-meeting-*.jar
```

### 查看日志
```bash
tail -f logs/application.log | grep chunk
```

### 清理临时文件（手动）
```bash
rm -rf uploads/.temp/*
```

### 查看上传统计
```bash
du -sh uploads/*/schedule/*/
```

### 数据库查询
```sql
-- 查看所有附件
SELECT * FROM conf_schedule_attachment WHERE deleted = 0;

-- 统计下载次数TOP 10
SELECT file_name, download_count FROM conf_schedule_attachment 
ORDER BY download_count DESC LIMIT 10;

-- 统计存储空间
SELECT tenant_id, SUM(file_size) as total_size 
FROM conf_schedule_attachment GROUP BY tenant_id;
```

---

**清单完成日期**: __________  
**清单完成人**: __________  
**最终状态**: ✓ 全部通过 / ⚠️ 部分未通过 / ✗ 需要返工

